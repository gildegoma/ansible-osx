
################################################
# Base Tools
################################################

- include: homebrew-extras.yml
  tags: homebrew-extras

- include: git.yml
  vars:
    git:
      user:
        fullname: 'Gilles Cornu'
        email:    'git@gilles.cornu.name'

################################################
# Virtualization Tools
################################################

- include: virtualbox.yml
  vars:
    virtualbox:
      version: 4.3.20
      build:   96996

- include: vagrant.yml
  vars:
    vagrant:
      version: 1.7.1

################################################
# IDE, Editor, ...
################################################

- include: intellij-idea.yml
  tags: intellij-idea
